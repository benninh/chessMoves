var withinBounds = function(x, y) {
  return (x <= 8 && x >= 0 && y <= 8 && y >= 0);
};

var moves = {
  pawn: 'placeholder',
  rook: 'placeholder',
  knight: 'placeholder',
  bishop: 'placeholder',
  queen: 'placeholder',
  king: 'placeholder'
};
